<!-- /templates/page.support.liquid -->

<div class="section-body">
  <div class="wrapper">
    <div class="grid">
      <div class="grid__item large--two-thirds push--large--one-sixth">
        <h2 class="section-title h1">{{ page.title }}</h2>

        <div class="rte">
          {{ page.content }}
        </div>
      </div>
    </div>
  </div>
</div>

{% section 'page-support' %}
