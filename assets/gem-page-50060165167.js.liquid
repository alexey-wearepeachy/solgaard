{% comment %}
	GEMPAGE BUILDER (https://apps.shopify.com/gempage)

	You SHOULD NOT modify source code in this page because
	It is automatically generated from GEMPAGE BUILDER
	Try to edit page with the live editor.
{% endcomment %}

        jQuery(function() {
            var $module = jQuery('#m-1592940339746').children('.module');
            $module.gfV1Countdown({
                id: "1592940339746",
                idSlug: "1592940339746"
            });
        });
    
        jQuery(function() {
  try {
    var $module = jQuery('#m-1592940339684').children('.module');   
    var navspeed = $module.data('navspeed'),
      autoplaytimeout = $module.data('autoplaytimeout'),
      autoplayhoverpause = $module.data('autoplayhoverpause'),
      navlg = $module.data('navlg'),
      navmd = $module.data('navmd'),
      navsm = $module.data('navsm'),
      navxs = $module.data('navxs'),
      collg = $module.data('collg'),
      colmd = $module.data('colmd'),
      colsm = $module.data('colsm'),
      colxs = $module.data('colxs'),
      dotslg = $module.data('dotslg'),
      dotsmd = $module.data('dotsmd'),
      dotssm = $module.data('dotssm'),
      dotsxs = $module.data('dotsxs'),
      marginlg = parseInt($module.data('marginlg')),
      marginmd = parseInt($module.data('marginmd')),
      marginsm = parseInt($module.data('marginsm')),
      marginxs = parseInt($module.data('marginxs'));

    var mode = jQuery('.gryffeditor').hasClass('editing') ? 'dev' : 'production';
    if(mode == 'production') {
      var autoplay = $module.data('autoplay'), 
          loop = $module.data('loop');
    } else {
      var autoplay = 0, 
          loop = 0;
    }

    var initCarousel = function() {
      $module.owlCarousel({
        mouseDrag: false,
        autoplayHoverPause: autoplayhoverpause,
        autoplay: autoplay,
        autoplayTimeout: autoplaytimeout,
        loop: loop,
        navSpeed: navspeed,
        autoWidth: !1,
        responsiveClass:true,
        responsive:{
          0:{
              items:colxs,
              nav: navxs,
              dots:dotsxs,
              margin: marginxs
          },
          768:{
              items:colsm,
              nav: navsm,
              dots:dotssm,
              margin: marginsm
          },
          992:{
              items:colmd,
              nav: navmd,
              dots:dotsmd,
              margin: marginmd
          },
          1200:{
              items:collg,
              nav: navlg,
              dots:dotslg,
              margin: marginlg
          }
        }
      });
    }
    
    // Fix nested carousel bug	
    if ($module.parent().parent().closest('.module-wrap[data-label="Carousel"]').length > 0) {	
        setTimeout(function() {	
            initCarousel();	
        }, 300)	
    } else {	
        initCarousel();	
    }
  } catch(err) {}
}); 
    
    
        jQuery(function() {
            var $module = jQuery('#m-1592940339744').children('.module');
        }); 
    
        jQuery(function() {
    var $module = jQuery('#m-1592940339638').children('.module');
    $module.gfV1Mailchimp();
});
    
    
    
    
    
    
        jQuery(function() {
            var $module = jQuery('#m-1592940440465').children('.module');
            $module.gfV1Countdown({
                id: "1592940440465",
                idSlug: "1592940440465"
            });
        });
    
        jQuery(function() {
            var $module = jQuery('#m-1592940943121').children('.module');
            $module.gfV1Countdown({
                id: "1592940943121",
                idSlug: "1592940943121"
            });
        });
    
        jQuery(function() {
    var $module = jQuery('#m-1592940779407').children('.module');
    $module.gfV1Mailchimp();
});
    
    
    
        jQuery(function() {
            var $module = jQuery('#m-1592940440405').children('.module');
        }); 
    
        jQuery(function() {
    var $module = jQuery('#m-1592940440369').children('.module');
    $module.gfV1Mailchimp();
});
    
    
    
    
    
    
    